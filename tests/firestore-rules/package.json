{
  "name": "revisley-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore security rules tests run against the Firebase emulator.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project=revisley-rules-test 'jest --runInBand'",
    "test:local": "jest --runInBand"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@types/jest": "^29.5.14",
    "firebase": "^12.7.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.0"
  }
}
